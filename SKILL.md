---
name: scan-project
copyright: 德贝信息技术部
description: >-
  扫描无文档旧项目，把分析与接手文件写入用户指定目录，供之后开发提高准确率。不预设语言与架构，只凭仓库证据识别技术栈、结构、风格并反推已有功能。扫描不改代码、不签出。项目路径与产出目录必填；库表与字段解释可选。路径齐后连续完成分析。当用户说「扫描项目」「分析项目」「接手旧项目」「摸清项目」时使用。禁止从代码反推表结构。
---

# 扫描项目（指定目录接手，供自主开发）

版权：德贝信息技术部

按仓库证据摸清现状，把接手文件写入用户指定目录，供之后开发提高准确率。扫描只摸清现状；开发时先读产出目录再改代码。

扫描前不确定语言、架构、目录结构。只认仓库证据；找不到写「未在代码中定位」。不套用 DDD / REST 等本仓没有的规范。本 skill **不反推建表**，产出不写被扫项目根。

当用户输入 **[扫描项目]** 或 **[分析项目]** 时，进入阶段 2。

对话示例见 [examples.md](examples.md)。取证清单见 [reference.md](reference.md)。

## 硬禁令

- 不改被扫项目里的源码（含不签出、不 `tf add`）。扫描也不对产出文件做 checkout / add。
- 未确认产出目录，不得写入任何接手文件。
- 不反推表结构。未提供库表文件则写「用户未提供」，不要从代码编表。
- 不从代码发明字段枚举含义。未提供解释文件则不要把数字写成业务名称。
- 不写真实密码、连接串、Token。配置只写路径与键名。
- 找不到的写「未在代码中定位」，不要编。
- 不写超出现有证据的架构叙述，不写全套需求文档，不写 `.cursor/rules`（除非用户点名）。

## 阶段节奏

- 阶段 2 只问缺项：项目路径、产出目录必填；库表与字段解释可选。
- 两项必填齐后，连跑阶段 3～7，中间可简报，不等「继续」。
- 仅当用户在阶段 2 写明「跳过阶段 N」才跳过该阶段；被跳过的节写「本阶段已跳过，未分析」。阶段 2 不可整段跳过。

```
Task Progress:
- [ ] 阶段2 收集路径（项目路径 + 产出目录必填）
- [ ] 阶段3 技术栈与启动项 → 技术栈.md
- [ ] 阶段4 最小地图 → 最小地图.md
- [ ] 阶段5 代码风格
- [ ] 阶段6 反推功能 → 推断功能.md
- [ ] 阶段7 写出 AGENTS.md（汇总，不改源码、不签出）
```

---

## 阶段 2：收集路径

缺必填问必填。不要猜测路径，不要从代码生成表结构或字段枚举文档。

1. **项目路径**（必填，可多条）：要扫的源代码根。前后端分仓则分行并标注用途。
2. **产出目录**（必填，须用户明确确认）：分析与接手文件只写这里，不写到被扫项目根。
3. **库表文件**（可选）：`.md` / `.sql` / 文本。不提供不阻塞。
4. **字段取值解释文件**（可选）：如 `ContractClassify`：`1` 橱柜、`2` 全屋。

用户给的若像子目录：可上探一层找 `.sln` / 多个工程；候选多于一个则列出请用户确认，不猜。

提供了库表或字段解释：打开并预览（表名/模块或字段取值示例即可，不要整文件粘贴），记下绝对路径。文件读失败则说明原因，或请用户声明不提供。

分析时忽略的目录：`node_modules`、`dist`、`build`、`bin`、`obj`、`.git`、`.vs`、`.idea`、`.svn`、`vendor`、NuGet `packages`、`__pycache__`、`target`、`.next`、`.nuxt`、`coverage`。

必填齐后进入阶段 3，不再停问。

---

## 阶段 3：技术栈与启动项

只认仓库文件（见 [reference.md](reference.md)）。版本只抄文件，抄不到写「未见」。锁文件 / 工程文件 > 清单 > README。多种技术分节写，不合成一套架构。

必须写下：

- 语言 / 运行时 / 框架及文件里出现的版本
- 包管理器与锁文件
- 实际启动项（Web / Job / 桌面 / 移动端分开列；不要把每个类库都当成可启动）
- 启动命令、测试命令（无则写未见）
- 缺本地 SDK 时列出「需要安装什么」，不擅自升级
- 配置文件路径与键名（无值）
- 仓库形态：单仓 / monorepo / 疑似分仓 / 多启动项

按 [templates/技术栈.md](templates/技术栈.md) 写入 `{产出目录}/技术栈.md`（目录不存在则创建）。源码路径用绝对路径。简报后进入阶段 4。

---

## 阶段 4：最小地图

地图必须能指到路径，找不到写「未在代码中定位」：

1. 进程入口
2. 实际目录分层（按现有文件夹名，不改成标准三层叫法）
3. 请求入口（HTTP / 桌面事件 / 消息 / 定时任务 / WCF / SignalR，有什么写什么）
4. 持久化方式（ORM / 手写 SQL / 文件 / 无）
5. 配置位置

按 [templates/最小地图.md](templates/最小地图.md) 写入 `{产出目录}/最小地图.md`。源码路径用绝对路径。简报后进入阶段 5。

---

## 阶段 5：代码风格

打开 2～3 个同类已有功能（优先完整链路：界面或入口 → 业务 → 数据）。不要选空壳，不要选 `*.Designer.cs`、`.g.cs`、T4、Swagger 客户端。找不到样本先问，不引入本仓库没有的框架。

只记代码里能看见的：命名、分层与命名空间、错误处理、鉴权（有才写）、SQL / ORM（有才写）、注释语言与密度、同步 / 异步。

结论写入对话并供阶段 7 摘进 AGENTS.md。简报后进入阶段 6。

---

## 阶段 6：反推功能（不反推表）

从代码列功能，每条带证据路径（绝对路径）与置信度（高/中/低）。禁止按「这类系统应该有什么」补功能。禁止写成正式需求规格。

取证顺序（都要打开文件，有什么用什么）：

1. 路由、菜单、页面/窗体文件名、权限码
2. HTTP API / 桌面命令入口 / 云函数
3. 后台 Job、消费者、Windows 服务
4. 状态枚举

菜单有、实现没有：低置信度。接口有、界面没有：标「仅入口」。两边都有：高。低置信度项列成待确认问题。

提供了库表：代码里出现文件中没有的表名，标「与用户表结构文件不一致，待确认」，不要补表设计。未提供库表：不对照、不反推。

提供了字段解释：取值对照该文件。未提供：不要把数字枚举写成业务名称。

按 [templates/推断功能.md](templates/推断功能.md) 写入 `{产出目录}/推断功能.md`。简报后进入阶段 7。

---

## 阶段 7：写出 AGENTS.md

按 [templates/AGENTS.md](templates/AGENTS.md) 写入 `{产出目录}/AGENTS.md`。已存在则增量更新。不签出、不改源码。

必须包含：

- 产出目录；各项目根用**绝对路径**；同目录产出用相对文件名（`技术栈.md`、`最小地图.md`、`推断功能.md`）
- 如何运行（启动 / 测试 / 需安装；阶段 3 已跳过则标明）
- 入口与分层（绝对路径；阶段 4 已跳过则标明）
- 请求与持久化
- 库表、字段解释：用户指定的绝对路径，或「用户未提供」
- 本仓风格要点（摘自阶段 5）
- 禁止事项、TFS（仅修改时签出）、注释强制、需求确认后再改

写完后给出产出目录下各文件路径，停止。不要开始改业务。

---

## 接手后改代码

扫描结束后，用户提出需求时（不在扫描阶段执行）：

1. 先读产出目录：`AGENTS.md`、`技术栈.md`、`最小地图.md`、`推断功能.md`。
2. 先整理**需求确认清单**（范围、影响的已有功能、将改文件绝对路径、是否动表——默认不动），停问用户确认后再改。
3. 每个将改的**已有源码文件**：先 `tf checkout <绝对路径>`（`tf.exe` 不在 PATH 则用 VS Team Explorer 下的全路径）。输出含 `locked for check-out by`、`checked out by`、`锁定`、`TF14098` 时，把占用人（及工作区）告诉用户，**不改该文件**。解析不到人名则贴 `tf` 原文，仍不改。
4. 新文件写入后再 `tf add <绝对路径>`。
5. 新增或改动的类、接口、方法、属性、字段以及关键业务逻辑必须写注释（语言随本仓，几乎无注释则用中文；写意图，不复述字面）。
6. 扫描阶段禁止 `tf checkout` / `tf add`。
