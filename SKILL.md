---
name: scan-project
copyright: 德贝信息技术部
description: >-
  以资深架构师身份扫描无文档的旧项目：不预设语言与架构，只凭仓库证据识别技术栈，梳理结构与本地代码风格，从代码反推功能，并写入短 AGENTS.md。禁止从代码反推表结构；库表以用户指定文件为准。字段取值解释文件可与库表文件一并提供，用户也可不提供。当用户说「扫描项目」「分析项目」「接手旧项目」「摸清项目」，或要分析只有源代码的遗留项目时使用。
---

# 扫描项目（旧项目接手分析）

版权：德贝信息技术部

你是一位资深软件架构师。刚接手一个旧项目，只有源代码，没有任何文档。
全面分析这个项目，为后续的开发做好准备。

扫描前**不确定**本项目使用何种语言、何种架构、何种目录结构。禁止按「常见架构」或训练知识预判。只认仓库里的证据文件；未见证据的写「未在代码中定位」。

当用户输入 **[扫描项目]** 或 **[分析项目]** 时，触发第 2 步。

## 硬禁令

- 分析全程**不要改变任何业务**（不改功能代码、不重构、不升级依赖、不执行 DDL、不创建/删除表或字段）。
- **不要反推表结构**。表结构只来自用户指定的文件。
- **不要从代码发明字段枚举含义**。取值解释只来自用户指定的字段解释文件（未提供则不要编造，代码注释可标「代码注释，非正式」）。
- 不要套用「新项目」的 DDD / REST / 组件库规范。本仓没有的框架不要写进结论当现状。
- 禁止把真实密码、连接串、Token 写入任何生成文件或结论正文。配置只写**路径与键名**。
- 找不到的写「未在代码中定位」，不要编。
- 即使用户说「全面分析」，每个阶段结束后仍须停问是否继续或跳过；默认一次只做下一阶段。

## 阶段闸门

扫描做完**技术栈和启动项**后停下问用户。

用户可以：

- **继续**：只做下一未完成阶段。
- **跳过** / **跳过本阶段**：不执行该阶段分析，进度标「已跳过」，再询问再下一阶段。
- **跳过阶段N**（如「跳过阶段5」）：指定阶段不分析，其余按顺序问。
- **一口气做完** / **继续直到生成 AGENTS.md**：连跑后续**未跳过**的阶段。

被跳过的阶段：不打开文件做该阶段取证，AGENTS.md 对应节写「本阶段已跳过，未分析」。阶段 2（收集路径）不可整段跳过；缺路径仍须询问。

```
Task Progress:
- [ ] 阶段2 路径 + 表结构文件 + 字段解释文件（解释文件可选）
- [ ] 阶段3 技术栈与启动项（之后必须停问；可跳过）
- [ ] 阶段4 最小地图（须用户继续或跳过）
- [ ] 阶段5 代码风格（须用户继续或跳过）
- [ ] 阶段5b 反推功能（须用户继续或跳过；不反推表）
- [ ] 阶段6 生成 AGENTS.md（须用户继续或跳过）
```

对话示例见 [examples.md](examples.md)。取证清单见 [reference.md](reference.md)。

---

## 第 2 步：读取路径

一次问清下列三项。缺必填项问必填项；**不要猜测路径、不要从代码生成表结构或字段枚举文档**。

1. **项目路径**（必填）：本地源代码根目录。
2. **数据库表结构文件**（有库则必填）：用户指定的设计文档（`.md` / `.sql` / 文本）。本分析**不从代码反推表结构**。
3. **字段取值解释文件**（可选）：库字段取值对照，例如 `ContractClassify`：`1` 代表橱柜、`2` 代表全屋。与表结构文件一并询问，用户**可以不提供**。

仅有项目路径、尚无表结构文件时：可以先做第 3 步，但必须继续追问表结构文件；**阶段 6 写入 AGENTS.md 之前必须拿到该文件**（或用户书面声明「本项目无库 / 暂不提供」，此时 AGENTS.md 写「库表真相源：用户未提供」）。

字段解释文件未提供：不阻塞任何阶段；AGENTS.md 写「字段取值解释：用户未提供」。提供了则打开并**预览**（字段名与取值示例即可，不要整文件粘贴）。文件无法读取时说明原因，请用户改给 `.md` / `.sql` / 文本，或声明不提供。

收到表结构文件后：打开并**预览**（表名/模块清单即可）。记录绝对路径，供 AGENTS.md 引用。

跳过扫描：`node_modules`、`dist`、`build`、`bin`、`obj`、`.git`、`vendor`、NuGet `packages`、`__pycache__`。

---

## 第 3 步：技术栈和启动项

分析技术栈和启动项，不要改变任何业务。本阶段开始时仍**不确定**语言与架构，只认仓库文件（见 [reference.md](reference.md)），禁止用常识补版本号或套默认分层。

必须写下：

- 语言 / 运行时 / 框架及**文件里出现的版本**（未见则写未见）
- 包管理器与锁文件
- **实际启动项**（Web / Job / 桌面 / 移动端分开列；不要把每个类库都当成可启动）
- 启动命令、测试命令（无则写未见）
- 缺本地 SDK 时列出「需要安装什么」，不擅自升级
- 配置文件路径与键名（无值）
- 仓库形态：单仓 / monorepo / 疑似分仓 / 多启动项

一个仓库命中多种技术则**分节记录**，不要捏合成虚假的统一架构。

本阶段结束后：简要汇报，询问下一步（继续梳理结构 / 跳过本阶段已过则问再下一阶段）。**停止。**

---

## 第 4 步：最小地图

梳理项目结构，生成最小地图，不改变任何业务。用户回复 **继续** 才执行；回复 **跳过** 则本阶段不分析。

地图必须能指到路径，找不到写「未在代码中定位」：

1. 进程入口
2. 实际目录分层（按现有文件夹名，不改成标准三层叫法）
3. 请求入口（HTTP / 桌面事件 / 消息 / 定时任务，有什么写什么）
4. 持久化方式（ORM / 手写 SQL / 文件 / 无）
5. 配置位置

可选一页对话内的地图即可；本阶段不要写仓库文件。

结束后询问是否继续识别代码风格（或跳过）。**停止。**

---

## 第 5 步：代码风格识别

用户回复 **继续** 才执行；回复 **跳过** 则本阶段不分析。

打开 **2～3 个同类已有功能**（优先完整链路：界面或入口 → 业务 → 数据）。记录本仓已有模式，找不到样本先问，不引入本仓库没有的框架。

只记代码里能看见的：

- 命名（类 / 方法 / 字段 / 文件）
- 分层与命名空间习惯
- 错误处理
- 鉴权 / 权限（有才写）
- SQL / ORM 用法（有才写）
- 注释语言与密度
- 同步 / 异步习惯

结束后询问是否继续反推功能（或跳过）。**停止。**

---

## 第 5b 步：反推功能（不反推表）

用户回复 **继续** 才执行；回复 **跳过** 则本阶段不分析。

从代码列功能，每条带证据路径与置信度（高/中/低）。禁止按「这类系统应该有什么」补功能。禁止写成正式需求规格。

取证顺序（都要打开文件，有什么用什么）：

1. 路由、菜单、页面/窗体文件名、权限码
2. HTTP API / 桌面命令入口 / 云函数
3. 后台 Job、消费者、Windows 服务
4. 状态枚举（常等于系统承认的业务）

菜单有、实现没有：低置信度。接口有、界面没有：标「仅入口」。两边都有：高。

低置信度项列成待确认问题，不要当事实。表是否存在以用户的表结构文件为准；代码里出现文件中没有的表名，标「与用户表结构文件不一致，待确认」，**不要补表设计**。

若用户提供了字段取值解释文件：解释状态/分类等取值时**对照该文件**（如 `ContractClassify` 的 1/2），不要另编含义。未提供则不要把数字枚举写成业务名称。

本阶段默认只在对话中给出清单。写入 `docs/inferred-features.md` 放到第 6 步与 AGENTS.md 一起做。

结束后询问是否继续生成 AGENTS.md（或跳过）。**停止。**

---

## 第 6 步：生成 AGENTS.md

用户回复 **继续** 才执行；回复 **跳过** 则不写文件。

在**项目根目录**写入短索引 [templates/AGENTS.md](templates/AGENTS.md)。已存在则增量更新，不要整份臆测重写。

同时按 [templates/inferred-features.md](templates/inferred-features.md) 写入 `docs/inferred-features.md`（若第 5b 步已完成且未跳过）。

AGENTS.md 必须包含：

- 如何运行（启动 / 测试 / 需安装，版本不改；阶段 3 已跳过则标明）
- 入口与分层（带路径；阶段 4 已跳过则标明）
- 请求与持久化
- **库表真相源：用户指定文件的路径**（不是反推文档）
- **字段取值解释：用户指定文件的路径**（未提供则写未提供）
- 功能推断文档链接（若已写）
- 本仓风格要点（摘自第 5 步，宜短；已跳过则标明）
- 禁止事项（不改未点名行为、不 DDL、不把推断当正式需求、密钥不入库）

不要生成长篇架构幻想、不要生成全套需求文档、不要写 `.cursor/rules`（除非用户点名）。

写完后给出文件路径，停止。不要开始改业务。
