# 对话示例

版权：德贝信息技术部

## 触发后缺少必填项

用户：

> [扫描项目]

Agent：

> 请补充以下信息（缺必填问必填；3、4 可不提供）：
>
> 1. **项目路径**（必填，可多条）：源代码根。前后端分仓请分行并标注。
> 2. **产出目录**（必填）：分析与接手文件只写这里，不写进被扫项目根。
> 3. **库表文件**（可选）：`.md` / `.sql` / 文本。不提供也可以，不会从代码反推表。
> 4. **字段取值解释文件**（可选）：如 `ContractClassify`：`1` 橱柜、`2` 全屋。
>
> 示例：
>
> ```
> 项目路径：D:\work\OldErp
> 产出目录：D:\work\OldErp-接手
> 表结构文件：D:\work\OldErp\docs\库表设计.md
> 字段解释文件：不提供
> ```

## 只给了项目路径

用户：

> [分析项目] 路径 E:\公司任务\OldErp

Agent：

> 项目路径已记录：`E:\公司任务\OldErp`。
>
> 还缺 **产出目录**（接手文件写入哪里）。库表与字段解释可选，不提供不阻塞。

## 路径齐后连跑（库表不提供）

用户：

> 项目路径：E:\公司任务\OldErp
> 产出目录：E:\公司任务\OldErp-接手
> 表结构不提供，字段解释不提供

Agent：记录路径与产出目录。库表、字段解释：用户未提供。不改源码、不签出。连跑阶段 3～7，写入 `E:\公司任务\OldErp-接手` 下的 `技术栈.md`、`最小地图.md`、`推断功能.md`、`AGENTS.md`。中间简报，不等「继续」。

## 前后端分仓 + 产出目录

用户：

> 项目路径：
> 1. E:\公司任务\OrderWeb （前端 Vue）
> 2. E:\公司任务\OrderApi （后端 WebAPI）
> 产出目录：E:\公司任务\订单接手
> 表结构文件：E:\公司任务\docs\库表设计.md

Agent：两个项目根分别记录。库表预览表名后，按路径分节写技术栈，不合成一套架构。全部产出写入 `E:\公司任务\订单接手`。`AGENTS.md` 里项目根与源码用绝对路径，同目录分析文件用相对名。

## 阶段 2 声明跳过

用户在给出路径时写「跳过阶段 5」。

Agent：阶段 5 不抽样风格。`AGENTS.md` 风格节写「本阶段已跳过，未分析」。其余阶段仍连跑。

## 开发时文件被占用（非扫描）

用户已扫完，提出改某接口。Agent 先读产出目录，列出需求确认清单，用户确认后执行 `tf checkout <绝对路径>`。输出含占用信息：

> 文件已被 **DOMAIN\zhangsan**（工作区 ZhangSan-PC）占用，未修改。请对方解锁或改其他文件。
