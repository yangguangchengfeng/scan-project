# 对话示例

版权：德贝信息技术部

## 触发后缺少路径（库表必填、字段解释可选）

用户：

> [扫描项目]

Agent：

> 请补充以下信息后再开始分析（缺必填问必填；第 3 项可不提供）：
>
> 1. **项目路径**（必填）：本地源代码根目录（含解决方案 / `package.json` / 工程文件的那一层）。
> 2. **数据库表结构文件**（有库则必填）：你指定的设计文档（`.md` / `.sql` / 文本）。本分析**不从代码反推表结构**，只使用你给的文件。
> 3. **字段取值解释文件**（可选）：字段取值对照，例如 `ContractClassify`：`1` 橱柜、`2` 全屋。不提供也可以继续，我不会从代码发明枚举含义。
>
> 示例回复：
>
> ```
> 项目路径：D:\work\OldErp
> 表结构文件：D:\work\OldErp\docs\库表设计.md
> 字段解释文件：D:\work\OldErp\docs\字段取值.md
> ```
>
> 没有字段解释文件时，第 3 行可省略或写「不提供」。
>
> 若当前工作区已经是该项目，也可以回复：「项目就用当前工作区；表结构文件是 xxx；字段解释不提供」。

## 只给了项目路径

用户：

> [分析项目] 路径 E:\公司任务\OldErp

Agent：

> 项目路径已记录：`E:\公司任务\OldErp`。
>
> 还缺 **数据库表结构文件** 路径。请提供（`.md` / `.sql` / 文本）。没有该文件时，我不会从代码发明表结构。
>
> **字段取值解释文件**可选。有则一并给出（取值对照，如某字段 `1`/`2` 的业务含义）；没有可写「不提供」。
>
> 示例：`E:\公司任务\OldErp\docs\库表设计.md`

## 库表有、字段解释没有：预览表结构后进入第 3 步

用户：

> 项目路径：E:\公司任务\OldErp
> 表结构文件：E:\公司任务\OldErp\docs\库表设计.md
> 字段解释不提供

Agent：

> 已打开表结构文件 `E:\公司任务\OldErp\docs\库表设计.md`（预览：文档中出现表 OrderHeader、OrderLine、SysUser … 共 N 张）。字段取值解释：用户未提供。接下来只根据代码分析技术栈和启动项，不预设语言或架构，不改任何业务。

（随后做第 3 步，汇报后停问：是否继续梳理项目结构？回复「继续」开始，或「跳过」不分析该阶段。）

## 三项都有

用户：

> 项目路径：E:\公司任务\OldErp
> 表结构文件：E:\公司任务\OldErp\docs\库表设计.md
> 字段解释文件：E:\公司任务\OldErp\docs\字段取值.md

Agent：

> 已打开表结构文件（预览表名 …）。已打开字段解释文件（预览：如 ContractClassify `1` 橱柜、`2` 全屋 …）。接下来按仓库证据识别技术栈，不改任何业务。

## 阶段闸门：继续

Agent（第 3 步结束）：

> 技术栈与启动项已记录（见上）。未改任何业务代码。
>
> 下一步是**梳理项目结构，生成最小地图**。回复 **继续** 开始；回复 **跳过** 则不分析本阶段；也可以先纠正上面的启动项。

用户：

> 继续

Agent：只做第 4 步，结束后再问是否继续识别代码风格（或跳过）。

## 阶段闸门：跳过

用户（在询问是否做第 5 步时）：

> 跳过

Agent：第 5 步不打开文件、不识别代码风格。进度标「阶段5 已跳过」。询问是否继续第 5b 步反推功能（或再跳过）。生成 AGENTS.md 时风格节写「本阶段已跳过，未分析」。

## 用户声明无数据库

用户：

> 表结构暂不提供，这是纯前端仓

Agent：在 AGENTS.md 写「库表真相源：用户未提供（纯前端 / 无库）」。字段解释若也未给，写「用户未提供」。不要反推表，不要编枚举含义。
