# 本目录给 Agent 的说明（扫描产出，供开发先读）

版权：德贝信息技术部

（由代码抽出。写不出的标「未在代码中定位」，不要编。禁止写入真实连接串或密码。语言与架构以仓库证据为准，不要套默认分层。）

开发时先读本目录：`技术栈.md`、`最小地图.md`、`推断功能.md`、本文件。改源码用下表**绝对路径**。同目录产出用相对文件名。

## 路径

- 产出目录：
- 项目根（绝对路径 + 版本管理 Git 或 TFVC，可多条）：
- 本目录分析文件：`技术栈.md`、`最小地图.md`、`推断功能.md`

## 如何运行

- 启动项目 / 命令：
- 测试：（无则写未见）
- 本机还需安装：（按工程文件 / 锁文件，不要改版本）

## 入口与分层

- 进程入口（绝对路径）：
- 目录含义：

详见 `最小地图.md`。

## 请求与数据

- 请求入口：
- 持久化：
- 连接串配置路径与键名（无值）：

详见 `技术栈.md`。

## 库表真相源（用户指定，非代码反推）

- 文件路径（绝对路径，未提供则写「用户未提供」）：
- 预览（表名/模块，非全文）：

## 字段取值解释（用户指定，可选；非代码反推）

- 文件路径（绝对路径，未提供则写「用户未提供」）：
- 用途：字段取值对照（例如 ContractClassify：1 橱柜 / 2 全屋）
- 预览（字段名与取值示例，非全文）：

## 功能推断（非正式需求）

- `推断功能.md`

## 本仓代码风格（摘自已有功能，勿改成新项目规范）

-

## 版本管理（按项目根，不按前后端角色）

扫描不签出、不 `git commit`。开发时看文件落在哪条项目根：

- **Git 根下**：直接改；不要 `tf checkout` / `tf add`
- **TFVC 根下**：改已有文件前 `tf checkout <绝对路径>`。输出含 `locked for check-out by`、`checked out by`、`锁定`、`TF14098`：提示占用人（及工作区），**不改该文件，并停止所有编码，暂停本轮对话，用户可输入 [继续] 再完成剩余编码**；解析不到人名则贴 `tf` 原文，仍不改。新文件写入后再 `tf add <绝对路径>`
- 不要用 git 命令代替 TFVC 签出；也不要对 Git 路径执行 `tf checkout`

## 开发前

1. 先读本目录上述文件。
2. 先整理需求确认清单（范围、影响的已有功能、将改文件绝对路径、是否动表——默认不动），用户确认后再改。

## 禁止事项

- 实现前先看同类已有功能，新代码与之同层、同命名、同错误处理。
- 修改代码前，先读取并理解当前文件的全貌，保持与现有代码风格一致，不随意变更格式。
- 禁止顺手重构、升级依赖、更换架构，除非用户明确要求。
- 禁止把本仓库没有的框架或分层套进来。
- 文档与代码冲突时以代码为准；代码未写明的不要写进说明里当事实。
- 修改 `{将改模块}` 时，若影响用户未点名的功能，先提示并确认。
- 库表以 `{库表文件路径或「用户未提供」}` 为唯一真相源（若已提供）。未提供则不反推表、不擅自 DDL。
- 字段取值含义以用户指定的字段解释文件为准（若已提供）；未提供则不要把数字枚举写成业务名称。
- 未经用户明确要求：不新建/删除数据库、不新增/删除数据库表、不新增/删除表字段、不执行 DDL。
- 新增或改动的类、接口、方法、属性、字段以及关键业务逻辑必须写注释。注释语言与本仓一致；几乎无注释则用中文。写意图与规则，不复述代码字面。
- 禁止使用任何魔法数字和魔法字符串。
- 有界面且用户要求时再用 Playwright 等做验收，不默认强制。
